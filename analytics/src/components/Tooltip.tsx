import React from 'react'
import { Tooltip } from 'recharts'

const TT: React.FC = () => {
  return (<Tooltip contentStyle={{ background: '#000', border: 0, borderRadius: 5 }} />)
}
export default TT
