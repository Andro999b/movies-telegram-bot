{
  "scripts": {
    "start": "sls offline --stage=dev --aws-profile serverless --debug *",
    "release": "sls deploy --aws-profile=serverless"
  },
  "dependencies": {
    "googleapis": "^105.0.0",
    "mongodb": "^4.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.13",
    "serverless": "^3.24.1",
    "serverless-content-encoding": "^1.1.0",
    "serverless-esbuild": "^1.33.1",
    "serverless-offline": "^11.2.3",
    "typescript": "^4.8.4"
  }
}