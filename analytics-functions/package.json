{
  "scripts": {
    "invoke:ga": "sls invoke local --function ga --aws-profile=serverless",
    "invoke:ga4": "sls invoke local --function ga4 --aws-profile=serverless",
    "invoke:mongostat": "sls invoke --function mongostat --aws-profile=serverless",
    "release": "sls deploy --aws-profile=serverless"
  },
  "dependencies": {
    "googleapis": "^105.0.0",
    "mongodb": "^4.8.1"
  }
}
